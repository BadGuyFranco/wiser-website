export const metadata = {
  title: "WISER Method Studio",
  description: "Content management for WISER Method",
};

export default function StudioLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
